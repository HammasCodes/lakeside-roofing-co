{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "astro",
  "buildCommand": "astro build",
  "outputDirectory": "dist",
  "installCommand": "npm ci",
  "headers": [
    {
      "comment": "Hero media lives in public/ with stable, non-hashed filenames, so it must NOT be marked immutable: re-running scripts/build-hero.mjs reuses the same names and immutable caches would serve the old clip for a year. Astro's own _astro/* output is content-hashed and already gets long-lived caching from Vercel.",
      "source": "/hero/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=604800, stale-while-revalidate=86400"
        }
      ]
    }
  ]
}
